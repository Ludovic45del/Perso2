[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = cible/tests
addopts = -v --tb=short
markers =
    unit: Unit tests (fast, no DB)
    integration: Integration tests (requires DB)
    e2e: End-to-end scenario tests
